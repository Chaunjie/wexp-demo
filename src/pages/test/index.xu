<template>
  <view bindtap="tapPro">{{text}}</view>
</template>
<script>
  import wexp from 'wexp/index'
  export default class Test extends wexp.page{
    config = {
      "navigationBarTitleText": "测试页面"
    }

    data = {
      text: 'chaunjie'
    }

    methods = {
      test () {
        console.log(99999)
      },
      tap (e) {
        console.log(e)
      }
    }

    onLoad (options) {
      const name = this.$parent.globalData.name
      this.setData({
        text: name
      })
      console.log(name)    
    }

    tapPro (e) {
      this.$back()
    }
  }
</script>